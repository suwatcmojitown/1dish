[phases.setup]
nixPkgs = ["php81", "php81Extensions.mysqli", "php81Extensions.pdo_mysql", "php81Extensions.mbstring", "php81Extensions.curl", "php81Extensions.gd", "php81Extensions.zip"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT -t /app"
